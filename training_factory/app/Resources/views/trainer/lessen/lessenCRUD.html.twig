{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>{{ title }}</h1>

                {{ form_start(lessenForm,{'attr': {'novalidate':'novalidate'}}) }}
                {{ form_widget(lessenForm) }}

                <button type="submit" class="btn btn-primary">{{ action }}</button>

                {{ form_end(lessenForm) }}

            </div>
        </div>
    </div>
{% endblock %}