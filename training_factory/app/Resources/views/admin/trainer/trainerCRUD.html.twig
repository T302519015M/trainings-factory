{% extends 'base.html.twig' %}

{% block body %}
    <div class="container header-fix">
        <div class="row">
            <div class="col-xs-12">
                <h1>{{ title }}</h1>

                {{ form_start(trainerForm,{'attr': {'novalidate':'novalidate'}}) }}
                {{ form_widget(trainerForm) }}

                <button type="submit" class="btn btn-primary">{{ action }}</button>

                {{ form_end(trainerForm) }}

            </div>
        </div>
    </div>
{% endblock %}
